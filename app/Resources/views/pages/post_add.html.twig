{% extends 'header.html.twig' %}




{% block body %}
	
	<a href="/post/">Back</a>
	<div class="container">
		<div class="card">
			<div class="card_body" style="padding:10px;">
				{{ form_start(form) }}
				{{ form_widget(form) }}
				{{ form_end(form) }}
			</div>
		</div>
	</div>
{% endblock %}